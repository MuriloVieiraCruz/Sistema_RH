/*
package com.senai.sistema_rh_sa.reports;

import net.sf.jasperreports.engine.*;
import net.sf.jasperreports.engine.data.JRBeanCollectionDataSource;

public class RelatorioPagamento {
    public static void main(String[] args) {
        try {
            // Carregar o arquivo JRXML e compilar para .jasper
            JasperReport jasperReport = JasperCompileManager.compileReport("seuarquivo.jrxml");

            // Preencher o relatório
            JasperPrint jasperPrint = JasperFillManager.fillReport(jasperReport, null, dataSource);

            // Exportar o relatório para PDF
            JasperExportManager.exportReportToPdfFile(jasperPrint, "folha_pagamento.pdf");
        } catch (JRException e) {
            e.printStackTrace();
        }
    }
}
*/
